BAJARILDI — v6.1 (bu fayl tarix uchun qoldirildi)
================================================================================

1) Kod tozalash (minifyEnabled + R8)  —  BAJARILDI
   Holati: `minifyEnabled true` + `shrinkResources true`, qoidalar
   `app/proguard-rules.pro` da.

   Qanday XAVFSIZ qilingani:
     * `-dontobfuscate` — sinf NOMLARI o'zgartirilmaydi. R8 ning eng xavfli
       qismi shu edi (refleksiya nom bo'yicha ishlaydi). Ishlatilmaydigan
       kodni olib tashlash esa qoladi — asosiy foyda shunda.
     * `-keep class uz.sevimli.tzd.** { *; }` — ilovaning O'Z kodiga
       umuman tegilmaydi.
     * Tink / androidx.security uchun aniq keep qoidalari — token
       shifrlash buzilmaydi.
     * `resources.getIdentifier(...)` butunlay olib tashlandi
       (MenuFunctions.icon endi `@DrawableRes Int`) — shusiz resurs
       tozalash dashboard ikonkalarini o'chirib yuborardi.
     * `proguard-android.txt` (optimize varianti EMAS) — ehtiyot uchun.

   QOLGAN ISH: birinchi release APK bitta terminalda to'liq sinalsin
   (sozlash, kirish, skan, приёмка, перемещение, hujjatlar, etiketka).
   Shundan keyin butun parkga tarqatilsin.

2) minSdk ni ko'tarish  —  QILINMADI (ataylab)
   minSdk = 21. Parkda Android 9 (m71) terminallar bor, ular ishlayapti.
   minSdk ni ko'tarish ularni yangilanishdan uzib qo'yadi. Zarurat yo'q.
