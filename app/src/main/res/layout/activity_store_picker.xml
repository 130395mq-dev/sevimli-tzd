<?xml version="1.0" encoding="utf-8"?>
<!--
  SKLAD TANLASH — 6.6 dizayni.

  O'zgargani: ilgari skladni bosgan zahoti ekran yopilardi. Xato bosilsa
  xodim qaytadan kirishga majbur edi. Endi avval tanlanadi (belgi ko'rinadi),
  keyin pastdagi "Saqlash" bosiladi.

  ID lar avvalgidek: btnBack, title, list, loading. Yangisi: btnSave.
  Sarlavha matni koddan almashtiriladi (Перемещение "Qayerga" deb yuboradi),
  shuning uchun standart qiymat O'ZGARTIRILMADI.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_header"
        android:paddingStart="6dp"
        android:paddingEnd="16dp"
        android:paddingTop="10dp"
        android:paddingBottom="16dp">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/j_ic_back"
            app:tint="@color/white"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/back" />

        <TextView
            android:id="@+id/title"
            style="@style/J.HeadTitle"
            android:layout_marginStart="4dp"
            android:text="@string/to_store" />
    </LinearLayout>

    <TextView
        style="@style/J.Sec"
        android:layout_marginStart="18dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="8dp"
        android:text="@string/which_store" />

    <ProgressBar
        android:id="@+id/loading"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:indeterminateTint="@color/brand"
        android:visibility="gone" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:overScrollMode="never">

        <LinearLayout
            android:id="@+id/list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingBottom="14dp" />
    </ScrollView>

    <!-- Asosiy amal doim pastda: bir qo'l bilan bosh barmoq yetadi.
         Sklad tanlanmaguncha o'chiq turadi. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSave"
        style="@style/J.Btn.Primary"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="14dp"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="14dp"
        android:enabled="false"
        android:text="@string/save_kt" />
</LinearLayout>
