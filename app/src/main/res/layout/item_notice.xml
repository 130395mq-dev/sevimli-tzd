<?xml version="1.0" encoding="utf-8"?>
<!--
  Bitta bildirishnoma qatori: ikonka + nom/izoh + son + strelka.
  Balandligi 72dp — qo'lqop bilan ham aniq bosiladi.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="72dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="14dp"
    android:paddingEnd="12dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/nIcon"
        style="@style/J.IconBox"
        app:tint="@color/brand"
        android:contentDescription="@string/section" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/nTitle"
            style="@style/J.Body.Bold" />

        <TextView
            android:id="@+id/nSub"
            style="@style/J.Sub"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/nCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="30dp"
        android:gravity="center"
        android:background="@drawable/j_badge"
        android:paddingStart="9dp"
        android:paddingEnd="9dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        android:fontFeatureSettings="tnum" />

    <ImageView
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="8dp"
        android:src="@drawable/ic_chevron_right"
        app:tint="#9BB3A7"
        android:contentDescription="@string/open" />
</LinearLayout>
